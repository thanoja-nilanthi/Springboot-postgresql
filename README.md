# Springboot project with postgresql
## To install the dependencies in springboot project type bellow command on terminal
```
mvn clean install
```
## Run the springboot project type bellow command on terminal
```
mvn spring-boot:run
```
